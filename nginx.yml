---
- name: Install and start the service
  hosts: all
  become: true
  tasks:
  - name: installing nginx
    apt:
     name: nginx
     state: present

  - name: starting nginx
    service:
     name: nginx
     state: started
     enabled: true
